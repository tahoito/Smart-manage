spring:
  datasource:
    url: jdbc:mysql://localhost:3306/HouseHoldAccount?serverTimezone=UTC
    username: root
    password: 168168
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8888
